#pragma once

class ResourceManager {
public:
	~ResourceManager();

private:
	ResourceManager();

	// Store bins for all types of information.
	// Entity bin
	// Level bin
	// Controller settings bin
};